%% -*- mode: erlang -*-
%% Slim plugin half of hecate-app-rag.

{erl_opts, [debug_info, warnings_as_errors]}.

{deps, [
    {hecate_sdk, "~> 0.6"},
    {macula,     "~> 4.7"},
    {cowboy,     "2.12.0"},
    {jsx,        "3.1.0"}
]}.

{shell, [{apps, [hecate_app_ragd]}]}.
